name: Android CI - 自動構建 APK

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
    - name: 📥 檢出代碼
      uses: actions/checkout@v4
      
    - name: ☕ 設置 JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle
        
    - name: 🔧 授予 Gradle 執行權限
      run: chmod +x gradlew
      
    - name: 🏗️ 構建 Debug APK
      run: ./gradlew assembleDebug --stacktrace
      
    - name: 📦 上傳 APK
      uses: actions/upload-artifact@v4
      if: success()
      with:
        name: GlucoWatch-Debug-APK
        path: app/build/outputs/apk/debug/*.apk
        retention-days: 30
        
    - name: ✅ 構建成功
      if: success()
      run: |
        echo "✅ APK 構建成功！"
        echo "📱 請在 Actions 頁面下載 APK 文件"
        echo "📍 路徑: app/build/outputs/apk/debug/app-debug.apk"
