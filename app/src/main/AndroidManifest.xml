<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 權限聲明 -->
    <!-- 讀取 xDrip+ 數據 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    
    <!-- 通知權限 (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- 前台服務 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    
    <!-- 鎖屏顯示 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- 震動 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- 開機自啟動 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- 錄音 (語音輸入) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <!-- 相機 (拍照記錄餐食) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    
    <!-- Wear OS 數據同步 -->
    <uses-permission android:name="com.google.android.permission.PROVIDE_BACKGROUND_LOCATION" />

    <application
        android:name=".GlucoWatchApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GlucoWatch"
        tools:targetApi="31">

        <!-- 主 Activity -->
        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GlucoWatch"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 鎖屏 Activity -->
        <activity
            android:name=".presentation.lockscreen.LockScreenActivity"
            android:exported="false"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.GlucoWatch.LockScreen"
            android:launchMode="singleInstance" />

        <!-- 飲食記錄 Activity -->
        <activity
            android:name=".presentation.meal.MealRecordActivity"
            android:exported="false"
            android:theme="@style/Theme.GlucoWatch"
            android:screenOrientation="portrait" />

        <!-- 鎖屏顯示服務 -->
        <service
            android:name=".service.LockScreenService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- 血糖警報服務 -->
        <service
            android:name=".service.GlucoseAlarmService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- 餐後偵測服務 -->
        <service
            android:name=".service.MealDetectionService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- 開機自啟動接收器 -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 警報接收器 -->
        <receiver
            android:name=".receiver.AlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- WorkManager -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>

        <!-- xDrip+ ContentProvider 查詢權限 -->
        <queries>
            <package android:name="com.eveningoutpost.dexdrip" />
        </queries>

    </application>

</manifest>